# Lab 3

This folder contains the deliverables for CSA Lab 3.

## Structure

- `algorithms/`
  - `sum_word_pstream.s`
  - `sum_word_pstream.yaml`
  - `rle_decompress.s`
  - `rle_decompress.yaml`
  - `reverse_string_cstr.s`
  - `reverse_string_cstr.yaml`

- `schema/`
  - `risc_iv_schema_final.drawio`

## Submission note

For the schema task, use an immutable GitHub link that points to the file inside a specific commit:

`https://github.com/<user>/<repo>/blob/<commit-hash>/lab3/schema/risc_iv_schema_final.drawio`
